# 小智笔记 - 数据库设计文档

---

## 1. 数据库概览

- **数据库类型**: MySQL 8.0 / PostgreSQL
- **ORM**: SQLAlchemy 2.0
- **命名规范**: 小写下划线命名 (snake_case)
- **字符集**: utf8mb4 (支持Emoji)
- **时区**: UTC+8

---

## 2. 数据表清单

| 序号 | 表名 | 说明 | 备注 |
|------|------|------|------|
| 1 | users | 用户表 | 存储用户基本信息 |
| 2 | articles | 文章表 | 存储文章内容 |
| 3 | categories | 分类表 | 文章分类 |
| 4 | tags | 标签表 | 文章标签 |
| 5 | article_tags | 文章标签关联表 | 多对多关系 |
| 6 | comments | 评论表 | 文章评论 |
| 7 | ai_conversations | AI对话表 | 豆包AI对话记录 |
| 8 | uploads | 文件上传表 | 存储上传文件信息 |

---

## 3. 详细字段设计

### 3.1 用户表 (users)

存储用户基本信息和账号数据。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键，用户唯一标识 |
| username | VARCHAR | 50 | 否 | - | 用户名，唯一 |
| email | VARCHAR | 100 | 否 | - | 邮箱，唯一 |
| phone | VARCHAR | 20 | 是 | NULL | 手机号 |
| password_hash | VARCHAR | 255 | 否 | - | 加密后的密码 |
| nickname | VARCHAR | 50 | 是 | NULL | 昵称 |
| avatar | VARCHAR | 500 | 是 | NULL | 头像URL |
| bio | TEXT | - | 是 | NULL | 个人简介 |
| role | TINYINT | - | 否 | 0 | 角色: 0-普通用户, 1-管理员 |
| status | TINYINT | - | 否 | 1 | 状态: 0-禁用, 1-正常 |
| is_verified | BOOLEAN | - | 否 | FALSE | 邮箱是否验证 |
| last_login_at | DATETIME | - | 是 | NULL | 最后登录时间 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引:**
- PRIMARY KEY (id)
- UNIQUE KEY uk_username (username)
- UNIQUE KEY uk_email (email)
- INDEX idx_status (status)
- INDEX idx_role (role)

---

### 3.2 文章表 (articles)

存储文章内容和元数据。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键，文章唯一标识 |
| title | VARCHAR | 200 | 否 | - | 文章标题 |
| slug | VARCHAR | 200 | 否 | - | URL友好的标识，唯一 |
| summary | VARCHAR | 500 | 是 | NULL | 文章摘要 |
| content | LONGTEXT | - | 否 | - | 文章内容 (Markdown/HTML) |
| content_type | TINYINT | - | 否 | 1 | 内容类型: 1-Markdown, 2-HTML |
| cover_image | VARCHAR | 500 | 是 | NULL | 封面图片URL |
| author_id | BIGINT | - | 否 | - | 作者ID，外键关联users |
| category_id | BIGINT | - | 是 | NULL | 分类ID，外键关联categories |
| status | TINYINT | - | 否 | 0 | 状态: 0-草稿, 1-已发布, 2-下架 |
| is_top | BOOLEAN | - | 否 | FALSE | 是否置顶 |
| view_count | INT | - | 否 | 0 | 阅读次数 |
| like_count | INT | - | 否 | 0 | 点赞次数 |
| comment_count | INT | - | 否 | 0 | 评论数量 |
| published_at | DATETIME | - | 是 | NULL | 发布时间 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 更新时间 |
| deleted_at | DATETIME | - | 是 | NULL | 软删除时间 |

**索引:**
- PRIMARY KEY (id)
- UNIQUE KEY uk_slug (slug)
- INDEX idx_author (author_id)
- INDEX idx_category (category_id)
- INDEX idx_status (status)
- INDEX idx_published_at (published_at)
- INDEX idx_is_top (is_top)
- FULLTEXT INDEX ft_title_content (title, content)  # MySQL全文索引

---

### 3.3 分类表 (categories)

存储文章分类信息，支持层级结构。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| name | VARCHAR | 50 | 否 | - | 分类名称 |
| slug | VARCHAR | 50 | 否 | - | URL标识，唯一 |
| description | VARCHAR | 255 | 是 | NULL | 分类描述 |
| parent_id | BIGINT | - | 是 | NULL | 父分类ID，自关联 |
| sort_order | INT | - | 否 | 0 | 排序顺序 |
| article_count | INT | - | 否 | 0 | 文章数量 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引:**
- PRIMARY KEY (id)
- UNIQUE KEY uk_slug (slug)
- INDEX idx_parent (parent_id)
- INDEX idx_sort (sort_order)

---

### 3.4 标签表 (tags)

存储文章标签信息。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| name | VARCHAR | 50 | 否 | - | 标签名称，唯一 |
| slug | VARCHAR | 50 | 否 | - | URL标识，唯一 |
| color | VARCHAR | 7 | 是 | NULL | 标签颜色 (HEX格式) |
| article_count | INT | - | 否 | 0 | 文章数量 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引:**
- PRIMARY KEY (id)
- UNIQUE KEY uk_name (name)
- UNIQUE KEY uk_slug (slug)

---

### 3.5 文章标签关联表 (article_tags)

文章和标签的多对多关联表。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| article_id | BIGINT | - | 否 | - | 文章ID |
| tag_id | BIGINT | - | 否 | - | 标签ID |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引:**
- PRIMARY KEY (id)
- UNIQUE KEY uk_article_tag (article_id, tag_id)
- INDEX idx_tag (tag_id)

---

### 3.6 评论表 (comments)

存储文章评论，支持嵌套回复。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| article_id | BIGINT | - | 否 | - | 文章ID |
| parent_id | BIGINT | - | 是 | NULL | 父评论ID，自关联 |
| user_id | BIGINT | - | 否 | - | 评论用户ID |
| content | TEXT | - | 否 | - | 评论内容 |
| like_count | INT | - | 否 | 0 | 点赞数 |
| status | TINYINT | - | 否 | 0 | 状态: 0-待审核, 1-已通过, 2-已拒绝 |
| is_author | BOOLEAN | - | 否 | FALSE | 是否为作者回复 |
| ip_address | VARCHAR | 45 | 是 | NULL | 评论者IP |
| user_agent | VARCHAR | 500 | 是 | NULL | 评论者UA |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 更新时间 |

**索引:**
- PRIMARY KEY (id)
- INDEX idx_article (article_id)
- INDEX idx_parent (parent_id)
- INDEX idx_user (user_id)
- INDEX idx_status (status)
- INDEX idx_created (created_at)

---

### 3.7 AI对话表 (ai_conversations)

存储用户与豆包AI的对话记录。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| user_id | BIGINT | - | 否 | - | 用户ID |
| session_id | VARCHAR | 64 | 否 | - | 会话ID，用于分组对话 |
| article_id | BIGINT | - | 是 | NULL | 关联文章ID（可选） |
| role | TINYINT | - | 否 | - | 角色: 1-用户, 2-AI |
| content | TEXT | - | 否 | - | 对话内容 |
| model | VARCHAR | 50 | 是 | NULL | 使用的AI模型 |
| tokens_used | INT | - | 是 | NULL | 消耗的token数 |
| context_refs | JSON | - | 是 | NULL | 引用的上下文信息 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引:**
- PRIMARY KEY (id)
- INDEX idx_user (user_id)
- INDEX idx_session (session_id)
- INDEX idx_article (article_id)
- INDEX idx_created (created_at)

---

### 3.8 文件上传表 (uploads)

存储用户上传的文件信息。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | 否 | 自增 | 主键 |
| user_id | BIGINT | - | 否 | - | 上传用户ID |
| original_name | VARCHAR | 255 | 否 | - | 原始文件名 |
| file_name | VARCHAR | 255 | 否 | - | 存储文件名 |
| file_path | VARCHAR | 500 | 否 | - | 文件存储路径 |
| file_url | VARCHAR | 500 | 否 | - | 文件访问URL |
| file_type | VARCHAR | 100 | 否 | - | MIME类型 |
| file_size | BIGINT | - | 否 | - | 文件大小(字节) |
| file_ext | VARCHAR | 10 | 否 | - | 文件扩展名 |
| usage_type | TINYINT | - | 否 | 1 | 用途: 1-文章封面, 2-文章内容, 3-头像 |
| article_id | BIGINT | - | 是 | NULL | 关联文章ID |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | 创建时间 |

**索引:**
- PRIMARY KEY (id)
- INDEX idx_user (user_id)
- INDEX idx_article (article_id)
- INDEX idx_usage (usage_type)

---

## 4. 数据库关系图

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│     users       │     │    articles      │     │   categories    │
├─────────────────┤     ├──────────────────┤     ├─────────────────┤
│ PK id           │◄────┤ FK author_id     │     │ PK id           │◄──┐
│    username     │     │ FK category_id   │────►│    name         │   │
│    email        │     │    title         │     │    parent_id    │───┘
│    password_hash│     │    content       │     └─────────────────┘
│    role         │     │    status        │
│    status       │     └──────────────────┘
└─────────────────┘              │
         │                       │
         │              ┌────────┴────────┐
         │              │  article_tags   │
         │              ├─────────────────┤
         │              │ PK id           │
         │              │ FK article_id   │
         │              │ FK tag_id       │
         │              └────────┬────────┘
         │                       │
         │              ┌────────▼────────┐
         │              │      tags       │
         │              ├─────────────────┤
         │              │ PK id           │
         │              │    name         │
         │              │    slug         │
         │              └─────────────────┘
         │
         │         ┌─────────────────┐
         │         │    comments     │
         │         ├─────────────────┤
         └────────►│ FK user_id      │
                   │ FK article_id   │
                   │ FK parent_id    │────┐
                   │    content      │    │
                   │    status       │    │
                   └─────────────────┘    │
                                          │
                                          └────(自关联)

┌─────────────────┐     ┌─────────────────┐
│ ai_conversations│     │     uploads     │
├─────────────────┤     ├─────────────────┤
│ PK id           │     │ PK id           │
│ FK user_id      │     │ FK user_id      │
│ FK article_id   │     │ FK article_id   │
│    session_id   │     │    file_url     │
│    content      │     │    file_size    │
│    tokens_used  │     │    usage_type   │
└─────────────────┘     └─────────────────┘
```

---

## 5. 数据库初始化SQL

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS xiaozhi_notes 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

USE xiaozhi_notes;

-- 用户表
CREATE TABLE users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    phone VARCHAR(20) DEFAULT NULL,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(50) DEFAULT NULL,
    avatar VARCHAR(500) DEFAULT NULL,
    bio TEXT DEFAULT NULL,
    role TINYINT NOT NULL DEFAULT 0 COMMENT '0-普通用户, 1-管理员',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '0-禁用, 1-正常',
    is_verified BOOLEAN NOT NULL DEFAULT FALSE,
    last_login_at DATETIME DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_username (username),
    UNIQUE KEY uk_email (email),
    INDEX idx_status (status),
    INDEX idx_role (role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 分类表
CREATE TABLE categories (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    slug VARCHAR(50) NOT NULL,
    description VARCHAR(255) DEFAULT NULL,
    parent_id BIGINT DEFAULT NULL,
    sort_order INT NOT NULL DEFAULT 0,
    article_count INT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_slug (slug),
    INDEX idx_parent (parent_id),
    INDEX idx_sort (sort_order),
    FOREIGN KEY (parent_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 标签表
CREATE TABLE tags (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    slug VARCHAR(50) NOT NULL,
    color VARCHAR(7) DEFAULT NULL,
    article_count INT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_name (name),
    UNIQUE KEY uk_slug (slug)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 文章表
CREATE TABLE articles (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    slug VARCHAR(200) NOT NULL,
    summary VARCHAR(500) DEFAULT NULL,
    content LONGTEXT NOT NULL,
    content_type TINYINT NOT NULL DEFAULT 1 COMMENT '1-Markdown, 2-HTML',
    cover_image VARCHAR(500) DEFAULT NULL,
    author_id BIGINT NOT NULL,
    category_id BIGINT DEFAULT NULL,
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0-草稿, 1-已发布, 2-下架',
    is_top BOOLEAN NOT NULL DEFAULT FALSE,
    view_count INT NOT NULL DEFAULT 0,
    like_count INT NOT NULL DEFAULT 0,
    comment_count INT NOT NULL DEFAULT 0,
    published_at DATETIME DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at DATETIME DEFAULT NULL,
    UNIQUE KEY uk_slug (slug),
    INDEX idx_author (author_id),
    INDEX idx_category (category_id),
    INDEX idx_status (status),
    INDEX idx_published_at (published_at),
    INDEX idx_is_top (is_top),
    FULLTEXT INDEX ft_title_content (title, content),
    FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 文章标签关联表
CREATE TABLE article_tags (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    article_id BIGINT NOT NULL,
    tag_id BIGINT NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_article_tag (article_id, tag_id),
    INDEX idx_tag (tag_id),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 评论表
CREATE TABLE comments (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    article_id BIGINT NOT NULL,
    parent_id BIGINT DEFAULT NULL,
    user_id BIGINT NOT NULL,
    content TEXT NOT NULL,
    like_count INT NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0-待审核, 1-已通过, 2-已拒绝',
    is_author BOOLEAN NOT NULL DEFAULT FALSE,
    ip_address VARCHAR(45) DEFAULT NULL,
    user_agent VARCHAR(500) DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_article (article_id),
    INDEX idx_parent (parent_id),
    INDEX idx_user (user_id),
    INDEX idx_status (status),
    INDEX idx_created (created_at),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    FOREIGN KEY (parent_id) REFERENCES comments(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- AI对话表
CREATE TABLE ai_conversations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    session_id VARCHAR(64) NOT NULL,
    article_id BIGINT DEFAULT NULL,
    role TINYINT NOT NULL COMMENT '1-用户, 2-AI',
    content TEXT NOT NULL,
    model VARCHAR(50) DEFAULT NULL,
    tokens_used INT DEFAULT NULL,
    context_refs JSON DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user (user_id),
    INDEX idx_session (session_id),
    INDEX idx_article (article_id),
    INDEX idx_created (created_at),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 文件上传表
CREATE TABLE uploads (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    original_name VARCHAR(255) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) NOT NULL,
    file_url VARCHAR(500) NOT NULL,
    file_type VARCHAR(100) NOT NULL,
    file_size BIGINT NOT NULL,
    file_ext VARCHAR(10) NOT NULL,
    usage_type TINYINT NOT NULL DEFAULT 1 COMMENT '1-文章封面, 2-文章内容, 3-头像',
    article_id BIGINT DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user (user_id),
    INDEX idx_article (article_id),
    INDEX idx_usage (usage_type),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 6. 数据库模型 (SQLAlchemy)

### 6.1 基础模型 (Base)

```python
from sqlalchemy import Column, BigInteger, DateTime
from sqlalchemy.sql import func

class BaseModel:
    """基础模型类"""
    id = Column(BigInteger, primary_key=True, autoincrement=True)
    created_at = Column(DateTime, server_default=func.now())
    updated_at = Column(DateTime, server_default=func.now(), onupdate=func.now())
```

### 6.2 用户模型

```python
from sqlalchemy import Column, String, Boolean, SmallInteger, DateTime, Text

class User(BaseModel):
    __tablename__ = "users"
    
    username = Column(String(50), unique=True, nullable=False)
    email = Column(String(100), unique=True, nullable=False)
    phone = Column(String(20), nullable=True)
    password_hash = Column(String(255), nullable=False)
    nickname = Column(String(50), nullable=True)
    avatar = Column(String(500), nullable=True)
    bio = Column(Text, nullable=True)
    role = Column(SmallInteger, default=0)  # 0-普通用户, 1-管理员
    status = Column(SmallInteger, default=1)  # 0-禁用, 1-正常
    is_verified = Column(Boolean, default=False)
    last_login_at = Column(DateTime, nullable=True)
```

---

**文档版本**: v1.0  
**创建日期**: 2026-04-13  
**数据库**: MySQL 8.0 / PostgreSQL + SQLAlchemy 2.0
